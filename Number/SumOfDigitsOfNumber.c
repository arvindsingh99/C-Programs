//Program to find sum of all digits of a number.
#include<stdio.h>
int main() {
    		if (fork() && (!fork())) {
       			 if (fork() || fork()) {
           						 fork();
        					        }
   					 }
  		  printf("1 ");
    		return 0;
	     }

  